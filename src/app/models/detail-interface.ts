export interface DetailInterface {
    id: number,
    check: string,
    monto: number,
    submonto: string,
    cuotas: string,
    fecha: string,
    mensual: number,
    tasa: string,
    boton: string
}
